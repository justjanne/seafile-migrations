databaseChangeLog:
- changeSet:
    id: V022_EmailUser
    author: seafile
    changes:
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `emailuser` MODIFY COLUMN `passwd` varchar(256)
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `emailuser` MODIFY COLUMN `passwd` varchar(256)
    - modifyDataType:
        tableName: emailuser
        columnName: passwd
        newDataType: varchar(256)
        dbms: postgresql
