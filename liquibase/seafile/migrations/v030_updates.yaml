databaseChangeLog:
  - changeSet:
      id: v030-create-garbagerepos
      author: seafile
      preConditions:
        - onFail: MARK_RAN
          sqlCheck:
            expectedResult: 0
            sql: "${table_exists_query} 'garbagerepos'"
      changes:
        - createTable:
            tableName: garbagerepos
            columns:
              - column: { name: repo_id, type: char(36), constraints: { primaryKey: true, nullable: false } }
