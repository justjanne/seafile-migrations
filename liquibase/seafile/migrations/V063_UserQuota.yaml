databaseChangeLog:
- changeSet:
    id: V063_UserQuota
    author: seafile
    changes:
    - dropPrimaryKey:
        tableName: userquota
    - addColumn:
        tableName: userquota
        columns:
        - column:
            name: id
            type: BIGINT
            autoIncrement: true
            constraints:
              primaryKey: true
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `userquota` MODIFY COLUMN `user` VARCHAR(255)
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `userquota` MODIFY COLUMN `user` VARCHAR(255)
    - modifyDataType:
        tableName: userquota
        columnName: user
        newDataType: VARCHAR(255)
        dbms: postgresql
    - dropNotNullConstraint:
        tableName: userquota
        columnName: user
        columnDataType: VARCHAR(255)
    - addUniqueConstraint:
        tableName: userquota
        columnNames: user
        constraintName: UK_UserQuota_user
    preConditions:
      onFail: CONTINUE
      not:
        columnExists:
          tableName: userquota
          columnName: id
    failOnError: false
