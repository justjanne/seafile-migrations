databaseChangeLog:
- changeSet:
    id: V063_Repo
    author: seafile
    changes:
    - dropPrimaryKey:
        dbms: postgresql
        tableName: repo
        constraintName: repo_pkey
    - dropPrimaryKey:
        dbms: mysql
        tableName: repo
    - addColumn:
        tableName: repo
        columns:
        - column:
            name: id
            type: BIGINT
            autoIncrement: true
            constraints:
              primaryKey: true
              nullable: false
    - addUniqueConstraint:
        tableName: repo
        columnNames: repo_id
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `repo` MODIFY COLUMN `repo_id` char(37)
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `repo` MODIFY COLUMN `repo_id` char(37)
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `repo` MODIFY COLUMN `repo_id` char(37)
    - modifyDataType:
        tableName: repo
        columnName: repo_id
        newDataType: char(37)
        dbms: postgresql
    preConditions:
      onFail: CONTINUE
      not:
        columnExists:
          tableName: repo
          columnName: id
    failOnError: false
