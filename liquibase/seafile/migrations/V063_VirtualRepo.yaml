databaseChangeLog:
- changeSet:
    id: V063_VirtualRepo
    author: seafile
    changes:
    - dropPrimaryKey:
        tableName: virtualrepo
    - addColumn:
        tableName: virtualrepo
        columns:
        - column:
            name: id
            type: BIGINT
            autoIncrement: true
            constraints:
              primaryKey: true
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `virtualrepo` MODIFY COLUMN `repo_id` CHAR(36)
    - sql:
        dbms: mysql,mariadb
        sql: ALTER TABLE `virtualrepo` MODIFY COLUMN `repo_id` CHAR(36)
    - modifyDataType:
        tableName: virtualrepo
        columnName: repo_id
        newDataType: CHAR(36)
        dbms: postgresql
    - dropNotNullConstraint:
        tableName: virtualrepo
        columnName: repo_id
        columnDataType: CHAR(36)
    - addUniqueConstraint:
        tableName: virtualrepo
        columnNames: repo_id
        constraintName: UK_VirtualRepo_repo_id
    preConditions:
      onFail: CONTINUE
      not:
        columnExists:
          tableName: virtualrepo
          columnName: id
    failOnError: false
