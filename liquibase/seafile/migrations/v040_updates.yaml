databaseChangeLog:
  - changeSet:
      id: v040-update-sharedrepo
      author: seafile
      preConditions:
        - onFail: CONTINUE
          not: { columnExists: { tableName: "${t_SharedRepo}", columnName: id } }
      changes:
        - addColumn:
            tableName: "${t_SharedRepo}"
            columns:
              - column: { name: id, type: int, autoIncrement: true, constraints: { primaryKey: true, nullable: false } }
        - modifyDataType: { tableName: "${t_SharedRepo}", columnName: from_email, newDataType: varchar(255) }
        - modifyDataType: { tableName: "${t_SharedRepo}", columnName: to_email, newDataType: varchar(255) }
        - createIndex:
            indexName: idx_sharedrepo_from_email
            tableName: "${t_SharedRepo}"
            columns:
              - column: { name: from_email }
        - createIndex:
            indexName: idx_sharedrepo_to_email
            tableName: "${t_SharedRepo}"
            columns:
              - column: { name: to_email }
